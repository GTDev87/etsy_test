installation

install nodejs
install npm

run: 
  npm install

run server on port 3001:
  node server

run on port 80:
  NODE_ENV=production node server

to run tests install mocha:
  npm install -g mocha

run test:
  mocha



This will hosted with docker-machine